<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VeterinaryClinic" parent="Theme.Material3.Light.NoActionBar">
    </style>

    <style name="Theme.VeterinaryClinic" parent="Base.Theme.VeterinaryClinic">
        <item name="background">@color/background</item>

    </style>

    <style name="Theme.VeterinaryClinic.Splash" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/splash_layer</item>
        <item name="android:logo">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.VeterinaryClinic</item>
    </style>

</resources>